import { getUserNotifications } from "@/lib/actions/notificationAction";
import { NEXT_AUTH } from "@/lib/auth";
import { getServerSession } from "next-auth";

export default async function (){
    
}